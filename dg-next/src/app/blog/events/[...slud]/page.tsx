import { Fragment } from "react";
import { NextPage } from "next";

interface EventFilterProps {}

const EventFilter: NextPage<EventFilterProps> = (props) => {
  return (
    <Fragment>
      <h1>filter</h1>
    </Fragment>
  );
};
export default EventFilter;
